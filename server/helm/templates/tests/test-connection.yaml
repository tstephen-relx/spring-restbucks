apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "restbucks.fullname" . }}-test-connection"
  labels:
    {{- include "restbucks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "restbucks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
