apiVersion: v1
kind: Service
metadata:
  name: {{ include "restbucks.fullname" . }}
  labels:
    {{- include "restbucks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restbucks.selectorLabels" . | nindent 4 }}
